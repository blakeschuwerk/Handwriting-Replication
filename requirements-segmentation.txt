opencv-python==5.0.0.93
numpy==2.4.6
scipy==1.17.1
pillow==12.3.0
pillow_heif==1.5.0

# Optional DL fallback engine (NOT required for default classical pipeline):
# python-doctr[torch]  -- only needed if invoking `segment.py --engine doctr`
